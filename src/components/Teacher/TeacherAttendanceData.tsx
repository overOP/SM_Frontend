import TeacherWorkspace from "./workspace/TeacherWorkspace";

/** Teaching workspace: attendance grid, marks spreadsheet, syllabus (Zustand + persisted context). */
const TeacherAttendanceData = () => <TeacherWorkspace />;

export default TeacherAttendanceData;
