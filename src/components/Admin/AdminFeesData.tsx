import FeeManagementDashboard from "./fees/FeeManagementDashboard";

/** Fee management — ledger, payment splitter, CSV-style flows (Vite + React; shadcn-style badges via CVA). */
const AdminFeesData = () => <FeeManagementDashboard />;

export default AdminFeesData;
